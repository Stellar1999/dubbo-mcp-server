spring:
  application:
    name: dubbo-mcp-server
dubbo:
  application:
    name: ${spring.application.name}
    qos-enable: false
  protocol:
    name: tri
    port: 50052
    triple:
      verbose: true
